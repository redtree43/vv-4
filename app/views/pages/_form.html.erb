<%= form_for :contact, url: contact_path, html: { class: 'contact-form'} do |f| %>
    <%= f.label :name %>
    <%= f.text_area :name %>
    <%= f.label :message %>
    <%= f.text_area :message %></br>
    <%= f.button :submit %>
<% end %>
